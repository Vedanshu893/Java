public class Solution {
    public Long solve(Long A, int B) {
        long ans=0;
        for(int i=0; i<B; i++){
           ans = ans | (1<<i);
           }
           
        ans = ~ans;
        A = A&ans;
        return A;
    }
}
